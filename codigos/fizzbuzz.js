
function fizzBuzzWithYourData (number) {

    if (number % 2 === 0) {
        return 'Mara';

    } else if (number % 3 === 0) {
        return 'Chacón';

    } else if (number % 7 === 0) {
        return 'Ortega';

    } else {
        return number.toString();
    }
}
